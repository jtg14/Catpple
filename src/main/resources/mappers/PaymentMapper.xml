<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="portfolio.mapper.PaymentMapper">

<select id="paymentList" resultType="vo.PaymentVO">
	select distinct p.* from payment p, corder o  where o.payment_pnum=p.pnum and o.member_mid=#{mId} order by p.pdate desc; 
	</select>
	
</mapper>